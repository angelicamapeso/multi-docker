language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t angelicamapeso/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true angelicamapeso/react-test npm test

after_success:
  - docker build -t angelicamapeso/multi-client ./client
  - docker build -t angelicamapeso/multi-nginx ./nginx
  - docker build -t angelicamapeso/multi-server ./server
  - docker build -t angelicamapeso/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push angelicamapeso/multi-client
  - docker push angelicamapeso/multi-nginx
  - docker push angelicamapeso/multi-server
  - docker push angelicamapeso/multi-worker
